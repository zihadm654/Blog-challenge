{
  "name": "starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": "true",
  "scripts": {
    "start": "npm run clean && parcel public/*.html --out-dir dev -p 3000",
    "build": "rimraf ./build && npm run clean && parcel build public/*.html --out-dir build --public-url ./",
    "clean": "rimraf ./dev && rimraf ./.cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/preset-env": "^7.15.0",
    "@babel/preset-react": "^7.14.5",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2",
    "sass": "^1.38.2"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
